import styled from "styled-components";

const Header = styled.header`
  background-color: #161D2A;
  color: #FFF;
  position: fixed;
  padding: 10px 0px;
  top: 0;
  left: 0;
  right: 0;
  z-index: 999;
  
  ul {
    display: flex;
    gap: 30px;
  }
 `

export const S = {
    Header
}