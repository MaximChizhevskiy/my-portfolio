import styled from "styled-components";

const TitleLogo = styled.span`
  margin-left: 15px;
  font-size: 30px;
  font-style: normal;
  font-weight: 500;
  line-height: normal;
`

export const S = {
    TitleLogo
}