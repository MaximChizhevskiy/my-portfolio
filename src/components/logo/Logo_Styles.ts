import styled from "styled-components";

const TitleLogo = styled.span`
  margin-left: 15px;
  font-size: 30px;
  font-style: normal;
  font-weight: 500;
  line-height: normal;
  color: white;
`

const LinkLogo = styled.a`
cursor: pointer;
`

export const S = {
    TitleLogo,
    LinkLogo
}